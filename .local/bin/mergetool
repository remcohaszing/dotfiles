#!/usr/bin/env bash
readonly LOCAL="$1"
readonly REMOTE="$2"
readonly BASE="$3"
readonly MERGED="$4"

if [[ "$MERGED" == yarn.lock ]]; then
  yarn
elif [[ "$MERGED" == package-lock.json ]]; then
  npm install
else
  code --new-window --wait "$MERGED"
fi

exit 1
